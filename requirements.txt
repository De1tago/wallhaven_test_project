# =============================================================================
# Wallhaven Viewer — зависимости
#
# Общее ядро (core/) работает на любой из этих библиотек, интерфейсы разделены:
#   * ui_gtk/  — GTK4 + Libadwaita (GNOME, большинство Linux)
#   * ui_qt/   — Qt6 / PySide6 + QML (Windows, KDE Plasma)
# =============================================================================

# --- Общие зависимости (ядро: API, кэш, установка обоев) ---
requests
dbus-python

# --- GTK4 / Libadwaita интерфейс (GNOME и др. Linux DE) ---
# Системные пакеты (Ubuntu):  python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1
PyGObject

# --- Qt6 / PySide6 интерфейс (Windows / KDE) ---
# Раскомментируйте для сборки Qt-версии:
PySide6

# --- Инструменты для сборки (нужны только разработчику) ---
pyinstaller
packaging
setuptools